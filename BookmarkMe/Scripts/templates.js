var demoTemplates = {
    demoItemTemplate: function (user, url, title) {
        return '<div>' + user + '</div>' +
                '<div>' +
                    '<div>' + url + '</div>' +
                '<div>';
    }
}